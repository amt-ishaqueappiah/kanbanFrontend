import UpdateTaskModal from "../components/UpdateTaskModal/UpdateTaskModal";

const ToBeDeleted = () => {
    return (
        <div>
            <button className="btn--primary btn--md">Add New Task</button>
            <button className="btn--danger btn--md">Add New Task</button>

            <button className="btn--danger btn--sm btn--block">
                Add New Task
            </button>

            <button className="btn--secondary btn--sm">Add Task</button>
        </div>
    );
};
